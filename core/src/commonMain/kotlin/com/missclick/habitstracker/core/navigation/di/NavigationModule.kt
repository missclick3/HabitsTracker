package com.missclick.habitstracker.core.navigation.di

import org.koin.dsl.module

val navigationModule =
    module {
        // Navigator is bound by the app module, which knows the start route.
    }
